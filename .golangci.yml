version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck      # unchecked errors
    - govet         # go vet suite
    - ineffassign
    - staticcheck   # correctness/style

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
